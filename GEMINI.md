# Antigravity — The White Mustang

<!-- Antigravity reads this file first, then falls back to AGENTS.md for shared guidelines. -->

## Antigravity-Specific Overrides

- When using the built-in browser to test pages, always check the mobile viewport
  (375 px wide) first before desktop.
- Prefer `pnpm` over `npm` unless a `package-lock.json` is present in the root.
- Do not auto-commit. Stage changes and present a diff summary for user review before
  any commit.
